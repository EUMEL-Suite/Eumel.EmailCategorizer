<Window x:Class="Eumel.EmailCategorizer.WpfUI.EmailSubjectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" Padding="4"
        Title="EmailSubjectWindow" Height="285" Width="535">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Category" Grid.Column="0" Grid.Row="0"/>
        <ComboBox Name="Category" IsEditable="True" Grid.Column="1" Grid.Row="0" />

        <TextBlock Text="Subject" Grid.Column="0" Grid.Row="1"/>
        <TextBox Name="MailSubject"  Grid.Column="1" Grid.Row="1" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2" Height="35">
            <Button Click="CancelButton">Cancel</Button>
            <Button Click="SendButton">Save</Button>
        </StackPanel>
    </Grid>
</Window>
